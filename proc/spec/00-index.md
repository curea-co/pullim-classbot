# 풀림(Pullim) 프로젝트 명세 인덱스

이 디렉토리(`proc/spec/`)는 풀림 프로젝트의 단일 진실원(Single Source of Truth)이다. 코드와 명세가 어긋나면 코드를 진실로 보고 spec을 갱신한다(`update-spec` 스킬 활용).

## 문서 구성

| # | 파일 | 다루는 내용 |
|---|------|----------|
| 01 | [01-ai-instruction.md](01-ai-instruction.md) | AI 명령지침 — 이 명세를 읽고 개발할 AI에게 전달할 행동 규칙 |
| 02 | [02-product-definition.md](02-product-definition.md) | 제품 정의 — 풀림 3축, 문제·목표·페르소나 |
| 03 | [03-features-and-ia.md](03-features-and-ia.md) | 핵심 기능·IA·사이트맵·라우트 매핑 |
| 04 | [04-ux-flow.md](04-ux-flow.md) | UX 플로우·시나리오·핵심 인터랙션 패턴 |
| 05 | [05-business-rules.md](05-business-rules.md) | 비즈니스 규칙·RBAC·ERD·검증 규칙·AI Tier |
| 06 | [06-content-data.md](06-content-data.md) | 콘텐츠 데이터셋·Mock 페르소나·시드 데이터 |
| 07 | [07-branding.md](07-branding.md) | 브랜드 네이밍·톤앤보이스·마이크로카피·UX writing(한자어/전문용어 정책) |
| 08 | [08-design-system.md](08-design-system.md) | 디자인 토큰·컬러·타이포·레이아웃·컴포넌트·버튼 어포던스 |
| 09 | [09-tech-stack.md](09-tech-stack.md) | 기술 스택·개발 환경·배포 정책 |
| 10 | [10-roadmap.md](10-roadmap.md) | 로드맵·Phase·통합 이력·검증 기준 |

### 기능별 명세 (Feature Spec)

01~10은 횡단(횡적) 카테고리, 11~는 추출본에서 추가로 만들어지는 도메인 기능 단위 명세이다.

| # | 파일 | 다루는 내용 |
|---|------|----------|
| 11 | [11-grading-hub.md](11-grading-hub.md) | 채점 허브 — AI 초안 + 교사 검수 + 루브릭 재학습 (`/teacher/grading`) |
| 12 | [12-student-assignment-solve.md](12-student-assignment-solve.md) | 학생 과제 풀이 — 클래스봇 내부 자급 워크스페이스 (`/classbot/assignment/*`) |
| 13 | [13-reports-and-emotion-checkin.md](13-reports-and-emotion-checkin.md) | 리포트 6종 + 감정 체크인 + 웰빙 지수 + 학부모 발송 (`/teacher/reports`, `/classbot/wellness`) |
| 14 | [14-teacher-assignment-workspace.md](14-teacher-assignment-workspace.md) | 교사 과제 내기 워크스페이스 — E2E 사이클의 진입점 (`/teacher/assignment/new`) |

## 참고 자료

- `input/docs-archive/` — 이전 11개 마스터/핸드오프 원문 (영구 보존, read-only)
- `input/storyboard-archive/` — 라이브러리 스토리보드 원문
- `input/design-prototype/` — 초기 React 프로토타입 (`pullim-study-demo-design/`의 사본)
- `proc/plan/` — 일자별 작업 계획·이력
- `proc/archive/` — 완료된 plan 격리

## 갱신 정책

- 명세 변경 시 `/update-spec`을 호출해 본 디렉토리만 수정
- 신규 명세는 `/create-spec`로 생성 (완전 신규 영역에 한정)
- 작업 계획은 `/update-plan`으로 `proc/plan/`에 누적

## 변경 이력

- **2026-09-15 (봇 아바타 emoji 예외를 데이터 계약으로 좁힌다 — 단일 진실원 표에는 emoji 열이 없다)**: `08 § 14.1.1 예외 2` 가 봇 아바타 emoji 를 통째로 예외에 넣고 근거를 「사용자 커스터마이징 가능 영역」이라 적어 두었는데, **세 곳에서 사실과 어긋났다.** ① **페르소나의 단일 진실원인 `07 § 4.6.2` 표가 emoji 를 한 번도 말하지 않는다** — 열은 `액센트 컬러 / 아바타 배경 / 모티프 / 어조 / 호칭` 다섯뿐이고, 그 표가 아바타에 넣으라고 지정한 것은 `√ x² ÷ 수식 도트`·`한글 자모 ㅎㄱㅁ` 같은 **모티프 글리프**다. 두 문서가 어긋났고 **단일 진실원이라고 스스로 적은 쪽을 따른다.** ② **커스터마이징이 실재하지 않는다** — `apps/classbot/components/builder/**` 에 emoji 픽커가 **없고**(전수 확인), `apps/classbot/lib/db/schema.ts:131` 의 `avatarEmoji` 가 `.notNull().default('🤖')` 라 서버가 만드는 새 봇은 전부 `🤖` 하나다. ③ **출처 결정문(`proc/archive/2026-05-07_inline-emoji-cleanup.md`)이 이 저장소에 없고**, 예외 2 의 위치 표기가 추출 이전 구조(`src/lib/mock/…`)를 가리켰다. **예외를 없애지 않고 좁혔다 — 데이터(DB 컬럼·직렬화·API 응답·mock 픽스처)는 보존하고 렌더만 뺀다.** 학생·교사 화면의 봇 아바타는 **과목 이니셜**(현재)·**모티프 글리프**(목표)로 그리고, 폴백만 lucide `Bot` 이다. 예외 2 **안의** 경로 표기만 이 저장소 기준으로 정정했고 예외 1·3·4·5·6 은 손대지 않았다. `07 § 4.6.2` 「룰」에 아바타 렌더 방식 한 줄을 명시했다. 인도: **`[예정]` #308**.
- **2026-09-15 (홈 4블록 4번에서 「웰빙 한 줄」을 뺀다)**: `04 § 9.1` 이 4번 블록을 「오늘의 KPI / 웰빙 한 줄」로 적고 있었으나, 그 자리를 실제로 채우는 것은 `growth-panel.tsx` 의 **「나의 성장」**(연속 학습일 · 주 평균 학습 시간 · 주간 히트맵)이다. 홈의 웰빙 넛지 카드는 **웰빙으로 가는 길이 아니었다** — `classbot-wellness-bot.ts:115` 가 `ctaHref` 를 언제나 `/classbot/chat?bot=…` 으로 채워서 `wellness-nudge.tsx:30` 의 `?? '/classbot/wellness'` 폴백이 한 번도 발동하지 않는다. **웰빙 기능 자체는 남는다**(`/classbot/wellness` 라우트 · 가벼운 모드 넛지). 봇 코멘트 카드의 자리를 정한 `13 § 9.3` 도 위치를 「웰빙 허브 헤더 하단 / 체크인 완료 직후」로만 적어 **홈을 포함하지 않는다**. **문서가 먼저 머지되고 홈에서 카드를 걷는 FE PR 이 그 뒤에 온다**(리뷰가 base 스냅샷 기준으로 수렴하게 — 2026-09-04 항목과 같은 방식).

- **2026-09-14 · 오후 (개발용 신원을 로컬 전용으로 — 배포에는 세울 DB 가 없다)**: `05 § 11.1` 의 경계 ① 이 `dev-classbot.pullim.ai` 를 고정 허용 목록에 두고 preview `*.vercel.app` 을 열어 두었다. **배포에 DB 가 있다는 전제**였는데 사실이 아니다 — Vercel 프로젝트 `pullim-classbot` 에 환경변수가 **하나도 없다**(`vercel env ls` 실측). 신원을 세우면 라우트가 `users` 를 조회하다 죽는다: 프리뷰에서 역할 전환으로 학부모를 누르면 `/api/parent/children` 이 **500 을 여섯 번** 내고 화면이 오류 카드로 끝난다(브라우저 실측). **익명일 때는 그 사슬이 시작도 안 한다** — 401 을 받고 mock·localStorage 로 돌며, 익명으로 확인한 다섯 화면은 전부 정상이다. 그래서 목록에서 배포 호스트를 뺀다. **경계 셋을 걷어내는 것이 아니라 ① 의 목록을 좁히는 것**이고 production 방어선은 그대로다. 노출 판정이 같은 함수를 쓰므로 전환 버튼도 배포에서 사라진다 — 버튼이 보이는 것 자체가 「눌러도 되는 길」이라는 약속인데 그 길 끝이 오류 카드다. **되열 조건**: 배포에 DB 가 붙는 날(BE 배선). 인도: **`[예정]` #297**. *(사용자 판단 — 「지금 모든 게 mock 인데 신원을 왜 넣고 있나」)*
- **2026-09-14 (인도 표기 전수 갱신 — 스택이 다 들어왔는데 명세가 「아직 안 들어왔다」로 적고 있었다)**: 스택 여덟(**#280·#281·#270·#283·#273·#269·#288·#291**)이 전부 `dev` 에 들어왔는데 명세 곳곳이 그것들을 **`[예정]`** 으로, 또는 **닫힌 #268·#271** 을 인도자로 지목한 채 남아 있었다. 낡은 명세는 리뷰의 판정 기준이 되어 **옳은 코드를 막는다** — 이 작업에서 여덟 번 겪은 자리다(#276·#282·#284·#285·#286·#289·#290·#287). **새 결정을 내리지 않는다 — 사실 정정과 인도 표기다.** 각 항목은 `origin/dev` 트리에서 **파일·라우트·마이그레이션 존재를 직접 확인**하고 인도 커밋까지 짚어 적었다.
  - **표기 관례** — `[예정]` 이 지워진 자리에 **`(#NNN · dev)`** 가 들어선다(#287 이 § 2.2 에서 쓰기 시작한 형식). 그 표기가 붙은 줄은 **현재 동작 설명**이고 「통과 기준」으로 읽지 않는다. `03` § 2 머리 박스에 이 뜻을 적었다.
  - **`03-features-and-ia.md`** — **§ 2.1** 학생 라우트 넷(`/classbot/{classroom,my-bots,discover,discover/[botId]}`)의 `[예정] #283` → **`(#283 · dev)`** + 레일 자리. `/classbot/discover` 는 mock 「공식 튜터 마켓」 서술을 걷고 **교사 공유 봇**으로. `/classbot/me/share` 의 `[예정]` → **`(#291 · dev)`**(서버 #288 · 레일 비노출). `/classbot/chat` 의 훅 이름을 **`useModeBots()` → `useStudentBots()`** 로(#283 이 모드 폐기와 함께 바꿨다). 메모 둘(「자기주도 진입」·「담은 봇·공부한 날의 출처」)을 현재 상태로. **§ 2.3** 학부모 트리 세 화면이 다 `dev`(#281·#291)임과 `Role` 3-way(#281)를 적고, **#268·#271 이 닫힌 뒤 실제 인도자**(#283/#281 · #288/#291)를 명시. 개발용 신원 쿠키의 `[예정] #266` → `dev`.
  - **`05-business-rules.md`** — **§ 11.1** 개발 전용 신원 폴백의 `[예정] #266` → `dev`. 그 자리가 「현재 동작 설명」이 되므로 **경계 ①** 도 실제 구현에 맞췄다: 「prod 호스트 거부 목록」 → **호스트 허용 목록 + fail-closed**(#277). **§ 11.2** `/parent/*` — `/parent/self-study` 의 `[예정]` → **#291**. **§ 11.4** 머리 박스 · 축 표의 `[예정]` 두 줄(`self_study_summary`·`class_assignment_summary`) · 2026-09-04 결정 단락 · 규칙 넷 머리 박스 — 전부 인도 완료로. 이 절은 이제 **통째로 현재 동작 설명**이고, 여기 적힌 규칙을 되돌리는 변경은 「아직 안 들어온 것」이 아니라 **회귀**다.
  - **`2026-06-23_classbot-dual-mode-design.md`** — 2026-09-04 박스의 「부품은 있고 문은 닫힌」·「서버 쪽 라우트는 아직 없다」를 걷고 표 다섯 줄을 전부 `dev` 로. 그 박스가 스스로 적어 둔 대로 **§6 Out 목록에서 세 줄을 실제로 걷었다**(BE persistence · real auth-scoped self-enrollment · teacher-side publish — 취소선 + 인도 번호). 남은 deferred 는 셋(custom tutors · IRT · cross-mode analytics). **§7 의 「Role stays `student | teacher`」** 도 사실과 달라 정정 — `dev` 의 `Role` 은 셋이고(#281) **자기주도가 셋째를 만든 것이 아니다**(그 제약이 막으려던 「모드를 Role 로 올리는 것」은 그대로 유효하다).
  - **`14-teacher-assignment-workspace.md` § 5.1** — 「대상 학생 1명 이상을 걷는다」의 `[예정] (#269)` → **인도됨**. `dev` 의 `assignment-form.tsx` 가 `rosterUnknown` 을 세우고 `targetValid = !rosterUnknown && (students.length === 0 || selectedIds.length >= 1)` 로 두는 것까지 확인해 적었다.
  - **`2026-07-03_m1-sso-identity-mapping.md` § 2.1** — 「`/parent/self-study` 만 `[예정]`」 → 세 화면 다 `dev`. **parent 거부 근거는 그대로다** — 화면 유무가 아니라 claim union + SSO 매핑이다.
  - **`2026-09-09_migration-numbering.md` § 4.1** — 그 절이 「순서가 바뀌면 고칠 것은 이 절이다」라고 적어 둔 대로. 「자기주도 축의 **남은 PR**(#288)」 → **머지됐고 정말로 마이그레이션 없이 들어왔다**. #291 도 `drizzle/**` 은 0줄이다 — 그래서 `dev` 는 여전히 `0000`~`0007` 이고 다음 빈자리는 `0008` 이다.
  - **리뷰 지적 둘을 반영했다 (라운드 1)** — ① **`05` § 11.1 경계 ①** 을 「허용 목록: … · PR preview 접미사」 한 줄로 적은 것이 **실제 경계보다 넓게 읽혔다.** 구현은 `.vercel.app` 이면 무조건 여는 것이 아니라 **배포 환경이 `preview` 라고 확인됐을 때만** 열고, **환경을 모르면 닫는다**(`VERCEL_ENV ?? NEXT_PUBLIC_VERCEL_ENV` · 서버 전용 값이 먼저). 그 접미사는 **production 배포도 받으므로** 순서를 뒤집은 positive 확인이 규칙의 핵심이고, 명세가 그것을 빠뜨리면 production 기본 URL 이 다시 열리는 회귀가 이 절을 근거로 통과한다. 판정 순서를 그대로 적었다. ② **`2026-06-23` §7 의 「FE-only, mock-first」** 가 **현재형 제약으로 남아** 같은 문서의 머리 박스·§6 과 정면으로 어긋났다 — 취소선 + 정정 박스로 갈랐다. **살아 있는 것은 괄호 안의 리포 규칙**(FE/BE 를 한 PR 에 섞지 않는다)이고, 죽은 것은 「아직 BE 가 없다」는 전제다. 같은 성격의 자리 둘을 함께 봤다 — **§4 절 제목**(「mock-first; BE is a separate later track」)과 **§7 의 「Each FE-only + small」**.
  - **fail-closed 의 범위를 좁혔다 (리뷰 지적 · 라운드 2)** — 라운드 1 이 「**배포 환경을 모르면 닫는다**」를 경계 ① 전체의 규칙으로 적었는데 **구현과 다르다.** `isDevIdentityHost` 는 **고정 허용 목록**(`localhost` · `127.0.0.1` · `::1` · `dev-classbot.pullim.ai`)을 **환경 확인 없이 통과시킨다** — `lib/__tests__/dev-identity.test.ts` 의 「배포 환경 모름」 케이스가 그 넷을 `true` 로 검증한다. 환경 미확인 시 닫히는 것은 **`*.vercel.app` 분기 하나**다(그 접미사만 production 배포도 받아 이름이 확인이 못 되기 때문). 그대로 뒀으면 **로컬 개발의 정상 동작을 금지하는 규칙**이 됐다 — `VERCEL_ENV` 가 없는 자리가 곧 로컬이다. 고정 목록은 「이름만으로 연다」로, fail-closed 는 「이 분기의 규칙이지 경계 전체의 규칙이 아니다」로 갈라 적었다.
  - **규범 문장 자체를 고쳤다 (리뷰 지적 · 라운드 3)** — `2026-06-23` §7 마지막 줄이 여전히 **`Each FE-only + small.`** 로 **단정**하고 있었다. 라운드 1 이 그 뜻이 끝났다고 **설명**만 하고 문장은 그대로 뒀는데, **문서를 독립적으로 읽거나 그 줄을 기준으로 검토하면 서버 PR 자체를 금지하는 규칙으로 다시 읽힌다.** 취소선 + **`Each one layer + small.`** 로 바꿨다 — 살아 있는 뜻은 리포 최상위 MUST 그대로다(**한 PR = 한 계층** · FE/BE 를 섞지 않는다 · 작게). **설명을 다는 것과 규범 문장을 고치는 것은 다르다** — 이번 PR 의 다른 자리에서도 같은 기준을 지켰는지 다시 봤다.
  - **그대로 둔 것** — `03` § 2.2(교사 라우트)는 **이미 `dev` 사실로 적혀 있어** 손댈 것이 없었다. `03` 의 `/teacher/bots{,/[botId]}` 「미구현」 표기는 **이 여덟이 인도한 것이 아니라** 별건(#243 계열) 사안이라 손대지 않았다. `11-grading-hub.md` § 1.5 · `12` 의 CTA 문구 `[예정]` 도 이 여덟과 무관해 그대로 둔다.
  - **범위** — `proc/spec/**` 문서만. 코드·`drizzle/**`·작업 가이드(`CLAUDE.md` 둘 · `AGENTS.md`)는 건드리지 않는다(가이드 편집은 별건 승인 사항 — 루트 `CLAUDE.md` § 4).

- **2026-09-10 · 오후 (⚠️ 앞 항목의 정정 — 오라클이 둘인데 하나만 봤다)**: 바로 아래 항목(**#289**, **머지됨**)이 § 11.4 규칙 2 의 단서를 고치면서 「**자기주도 축은 미동의 자녀를 결과 집합에서 뺀다**」로 적었다. **그 판이 틀렸고, 틀린 채로 `dev` 에 들어갔다.** 여기 그대로 남겨 둔다 — 명세가 한 번 잘못된 모양을 권위로 세웠다는 사실 자체가 다음 사람이 알아야 할 이력이다.
  - **무엇을 놓쳤나** — 누출 경로가 **둘**인데 하나만 봤다. ① **`null` 오라클**: 미동의 자녀를 실으면서 `scope_label`·`expires_at` 처럼 **동의 행에서만 오는 값**을 함께 실으면 `scopeLabel === null` 이 곧 「동의 안 함」이 된다. ② **대조 오라클**: 미동의 자녀를 **결과 집합에서 빼면** 학부모가 `GET /api/parent/children`(연결 자녀 전원)과 축별 응답을 **대조해** 빠진 자녀 = 미동의 자녀를 알아낸다. #289 는 ① 을 「자녀 제거」로 막았고, **그 순간 ② 가 열렸다** — 규칙 2 본문이 막으려던 바로 그 구별이다. Codex 가 #288 라운드 5 에서 ② 를 짚었고(`self-study/route.ts:98` 인라인), 코디네이터가 같은 판단을 내렸다.
  - **바른 답은 셋째였다** — ① 의 출처는 **동의 정보**이지 자녀 행이 아니다. 그러니 **자녀는 전원 싣고**, 내용은 동의 술어를 조회 조건 안에 넣어 미동의면 **빈 배열**이 되게 하고, **범위 라벨·만료는 학부모 응답에서 뺀다.** 그러면 미동의 자녀와 「동의했지만 활동 0」 자녀의 응답이 **완전히 같은 모양**이 되어 ①·② 가 동시에 막힌다. **이 모양은 새로 만든 것이 아니다** — #280 의 `GET /api/parent/children` 이 이미 정확히 그 구조다(전원 + 게이트된 읽기 + 빈 배열 · 동의 정보 없음). 규칙이 라우트마다 다르게 나타날 이유가 없어졌다.
  - **고친 자리** — `05` § 11.4 **규칙 2**: #289 가 넣은 표(「목록은 `/api/parent/children` 이 지고, 축별 라우트는 목록을 지지 않는다」)와 「자기주도 축은 결과 집합에서 빠진다」 서술을 **걷고**, 오라클 둘을 나란히 적은 뒤 세 줄 표(자녀 목록 = 전원 / 축별 내용 = 게이트된 조회 → 빈 배열 / 동의 정보 = 응답에 없음)로 바꿨다. 「가리지 않는다」의 주체도 **모든 학부모 라우트**로 넓혔다. 공유 범위·만료가 필요한 곳은 **학생 자기 화면**(`/classbot/me/share`)이라는 것도 못박았다.
  - **인도** — **#288**(서버)이 이 모양으로 고쳐 인도하고, 화면 PR 이 뒤따른다. #288 이 라운드 3·4 에서 넣은 둘(봇·학습일 조회의 동의 술어 · 부여의 트랜잭션 직렬화)은 **그대로 살린다** — 이 정정과 무관하게 맞는 수정이다.
  - **범위** — `proc/spec/**` 문서만.

- **2026-09-10 (학부모 열람의 인도 표기 · 「자녀 목록은 가리지 않는다」의 주체)**: § 11.4 가 세운 것 중 **#280·#281 이 인도한 것들이 아직 `[예정]` 로 적혀** 있었고(「`revoked_at` 은 `dev` 의 `consent_logs` 에 없다」 — 그 칼럼은 `0005_breezy_anita_blake` 로 이미 있다), 자기주도 인도는 **닫힌 #271** 을 가리키고 있었다. 그리고 규칙 2 의 단서가 **주체를 적지 않아** 리뷰가 그것을 근거로 #288 을 막았다. **새 결정을 내리지 않는다 — 사실 정정과 인도 표기다**(#276·#282·#284·#285·#286 과 같은 상황이고 같은 방식으로 푼다).
  - **`[⚠️ 2026-09-10 오후 정정됨 — 이 줄의 결론이 틀렸다. 위 항목을 읽어라.]`** **규칙 2 의 단서에 주체를 적었다** — 「자녀 목록(이름·관계) 자체는 가리지 않는다」를 지는 라우트는 **`GET /api/parent/children` 하나**다. 종전 문장은 축별 라우트마다 목록을 다시 실어야 한다는 뜻으로도 읽혔는데, **그렇게 하면 규칙 2 의 단서가 규칙 2 의 본문을 깬다** — 축별 응답에 미동의 자녀를 실으면 그 자리의 빈 값(특히 동의 행에서만 오는 `scope_label`·`expires_at` 의 `null`)이 **곧 「동의 안 함」**이 된다. 그래서 표로 갈랐다: 목록은 `/api/parent/children` 이 지고 **가리지 않는다**, 축별 내용은 그 축의 조회가 지고 **애초에 읽지 않는다**(규칙 1). 반·과제는 목록과 같은 라우트라 「이름은 실리고 내용은 빈 배열」로, 자기주도는 다른 라우트라 「결과 집합에서 빠진다」로 **같은 규칙이 다르게 나타난다** — 목록을 가리는 것이 아니라 그 라우트가 목록을 지지 않는 것이고, 「이어진 자녀가 없다」와 혼동될 자리도 없다(그 화면은 자녀 유무를 `/api/parent/children` 에서 읽는다).
  - **인도 표기** — `05` § 11.2 · § 11.4(머리 박스 · 규칙 3 · 규칙 넷 머리 · 2026-09-04 결정 단락) · `03` § 2.1(`/classbot/me/share`) · § 2.3(학부모) · `2026-06-23` 박스의 「학부모 × 자기주도」 줄. **`dev` 에 있는 것**으로 옮긴 것: `consent_logs` 축 둘 · `revoked_at`(#280) · `GET /api/parent/children` 의 반·과제 게이트(#280) · `app/(parent)` 트리(#281). **`[예정]` 로 남는 것**: `/api/parent/children/self-study` · `app/api/me/consents/*` → **#288**(서버), 화면(`/parent/self-study` · `/classbot/me/share`) → 뒤따르는 별건 PR.
  - **#271 → #288 로 갈랐다** — #271 은 **서버+화면을 한 PR** 에 담아 리포 최상위 규칙(FE/BE 분리)에 걸렸고, head 가 시리즈 전체(45 커밋)를 지고 있어 base 를 `dev` 로 옮기면 이미 머지된 것들이 diff 에 되돌려 실려 **리뷰가 수렴할 수 없었다.** 그래서 닫고 계층별로 다시 냈다.
  - **파생 명세의 「현재 상태」도 같이 맞췄다** (리뷰 지적 · 라운드 1) — 한 사실을 여러 문서가 **현재 상태로** 서술하고 있어서, 한 곳만 고치면 문서끼리 다른 구현 상태를 지시한다. 셋을 함께 고쳤다: ① `04-ux-flow.md` UC-P1 1단계의 「`/parent` 는 `[예정]` #268 · `dev` 에 없다」 → **#281 로 `dev` 에 있다**. ② `2026-07-03_m1-sso-identity-mapping.md` § 2.1 의 parent 거부 근거에서 「화면 자체는 아직 `dev` 에 없다(#268·#271)」를 걷었다 — **화면 유무는 더 이상 거부 근거가 아니다**(남은 근거는 처음부터 claim union + SSO 매핑이었고 그대로다). ③ `2026-09-09_migration-numbering.md` **§ 4** — 그 절이 스스로 「순서가 바뀌면 고칠 것은 PR 이 아니라 이 절이다」라고 적어 둔 대로 고쳤다. **§ 4.1(현재)** 은 `dev` 가 **`0000`~`0007`**(다음 빈자리 `0008`)이고 `0005`=`revoked_at`(#280) · `0006`=`self_enrollments` · `0007`=`self_study_days`(#270)임을 적는다 — **규칙 1·2 가 예측한 순서가 실제로 나왔다**(§ 3.1 의 C). **§ 4.2** 는 2026-09-09 당시의 예측을 **이력으로** 남긴다. #288 에 마이그레이션이 없다는 것도 함께 적었다(그 브랜치가 들고 있던 `0007_silent_nick_fury.sql` 을 걷어낸 근거 포함).
  - **범위** — `proc/spec/**` 문서만. 코드는 건드리지 않는다. **작업 가이드(`CLAUDE.md` 둘 · `AGENTS.md`)에도 같은 낡은 표기가 남아 있으나 이 PR 이 손대지 않는다** — 그 파일 편집은 별건 승인 사항이다(루트 `CLAUDE.md` § 4).

- **2026-09-10 (담은 봇·공부한 날의 서버 정본 — 규칙은 적혔는데 인도가 안 적혀 있었다)**: 명세는 `self_enrollments`·`/api/me/self-bots` 를 **실명 범위 정본**으로 두고 **비로그인에만 localStorage 폴백**을 허용한다고 적어 두었으나, **그 전환을 어느 층이 지느냐**를 적지 않았다. 그래서 그 파일을 스치는 PR 마다 규칙이 걸렸다 — **#283**(학생 화면)이 열다섯 라운드째 같은 지적으로 막혔다(「자기주도 봇·학습일 기록이 계정 범위 서버 API 와 연결되지 않아…」 · 인라인은 `hooks/api/self-bots.ts`). **지적의 최종 상태 요구는 맞다. 틀린 것은 인도 배정이다** — 그 일은 **#273**「담은 봇·공부한 날의 출처를 서버로 갈아탄다 — 훅·스토어 (5b/7)」의 몫이고, 그 PR 의 변경 파일에 `hooks/api/self-bots.ts` 와 `lib/store/self-learning.ts` 가 실제로 들어 있다(`gh api repos/curea-co/pullim-classbot/pulls/273/files`). **새 결정을 내리는 것이 아니라 이미 정해진 층 분리를 문서에 적는다** — #276·#282·#284 와 같은 상황이고 같은 방식으로 푼다.
  - **배정** — ① **서버**(`self_enrollments` · `self_study_days` · `/api/me/self-bots` · `/api/me/study-days`+백필)는 **#270** 으로 **`dev` 에 이미 있다**. ② **훅·스토어의 「식별된 사용자 = 서버 정본 / 비로그인은 서버를 부르지 않고 localStorage」 전환**은 **`[예정]` #273** 이 인도한다(화면 파일 0개). **폴백 조건은 「비로그인」이지 「`401`」이 아니다** — 세션 만료·무효 토큰까지 폴백으로 넘기면 옛 로컬 기록이 정본 자리로 돌아와 **계정 범위 분리가 깨진다**(2026-09-04 박스의 원래 규칙 그대로 옮겨 적은 것이다). ③ 그래서 **#283**(학생 화면)은 **로컬 폴백 상태로 온다** — 화면 단위 PR 이 그 전환을 함께 하면 층이 섞이고(루트 `CLAUDE.md` 최상위 MUST 「한 PR = 한 계층」) **#273 의 몫이 통째로 사라진다.**
  - **`[예정]` 의 뜻은 그대로다** — **`dev` 에 없는 것**이고 **그 PR 이 통과해야 할 기준**이다(아래 2026-09-04 항목 「읽는 법」). 그러니 **`[예정]` #273 의 기준을 #283 의 기준으로 읽지 않는다.**
  - `2026-06-23_classbot-dual-mode-design.md` **2026-09-04 박스** — 표의 「BE persistence (자기주도)」 줄을 `[예정]` → **`dev` 에 있다**(#270 머지)로 고치고 「표와 라우트까지」라는 경계를 적었다. 「real auth-scoped self-enrollment」 줄(localStorage 폴백 규칙이 적힌 자리)에는 **그 갈림을 FE 에서 가르는 것은 #273** 임을 달았다. 그리고 **「자기주도 데이터 출처(훅·스토어)」 줄을 신설**해 #273 을 명시했다. 박스 끝에 이음매 단락을 더해 세 층(서버 #270 / 훅·스토어 `[예정]` #273 / 화면 #283)을 갈라 적었다. **번호는 적지 않는다**(2026-09-09 항목의 규칙 그대로).
  - `2026-06-23_classbot-dual-mode-design.md` **2026-09-09 박스 「인도」 줄** — 서버 목록에 **#270** 을 더하고, **그 화면의 데이터 출처는 아직 localStorage** 임을 적었다. ④(옛 저장값)도 갈랐다 — #283 이 세우는 것은 v1 의 **모양**이고 **출처를 서버로 옮기는 것은 #273** 이다.
  - `03-features-and-ia.md` **§ 2.1** — 「담은 봇·공부한 날의 출처」 메모 신설. `/classbot/my-bots` · `/classbot/discover{,/[botId]}` 에 붙은 **`[예정] #283` 은 화면까지**라는 경계를 적었다.
  - **범위** — `proc/spec/**` 문서만. **코드는 한 줄도 건드리지 않는다.** 명세의 **결정 내용**도 바꾸지 않았다 — 바꾼 것은 **인도 배정 표기**뿐이다.

- **2026-09-09 (자기주도는 「모드」가 아니라 「장소」다 — Locked decision 2·4 를 갈아 끼운다)**: 명세는 자기주도를 **전역 토글**로 세워 두었고(`2026-06-23` Locked 2), `dev` 는 그 위에 세운 구현이 「부품은 있고 문은 닫힌」 상태로 굳어 있다. 문을 여는 방법을 다시 보다가 **토글을 되살리지 않고 갈 곳을 만드는 쪽**으로 정해졌다. **코드가 먼저 그리 갔고**(#268 → 리베이스본 #283) 명세가 뒤따르지 않아, 리뷰가 낡은 Locked decision 을 근거로 그 PR 을 막았다. 문서를 사실에 맞춘다.
  - **왜 토글이 아닌가** — 전역 토글은 **화면마다 뜻을 바꾼다**(같은 `/classbot/chat` 이 모드에 따라 다른 목록을 그린다). 그리고 그 토글을 못 찾은 학생에게 마켓에서 담은 봇은 **어느 화면에서도 열리지 않는 진열장**이 된다. 대신 `/classbot/my-bots` 라는 **레일에 상시 자리를 가진 장소**를 두고, `/classbot/chat` 은 반 봇과 담은 봇을 **한 목록**으로 그린다(같은 봇이 양쪽이면 한 번만 싣고 **반 관계가 이긴다** — 과제·교사 관제가 거기 붙는다).
  - **왜 공식 튜터가 아닌가** — `ot_*`(`lib/mock/classbot-official.ts`)는 **mock 이고 서버에 대응 행이 없다.** `chat_messages.bot_id` 가 `class_bots` 를 FK 로 물어 **그 봇과의 대화는 애초에 저장될 수 없었다.** 반면 #267 이 `class_bots.is_published` 와 `/api/marketplace/bots` 를 이미 `dev` 에 들였다. 그래서 마켓의 정본을 **교사가 공유한 봇**으로 옮긴다. student-created/custom 튜터는 **여전히 deferred** 다.
  - **무엇이 그대로인가** — Locked 1(한 서비스)·3(teacher-less 학생도 혼자 선다)·5(목표/경로 학습 모델). **3 이 개정의 이유 중 하나다**: 선생님이 없는 학생에게 참여 코드가 유일한 입구이면 막다른 길이라, 마켓이 그 학생의 입구가 된다. `/classbot/learn/*` 는 mock `ot_*` 위에서 그대로 돈다 — P5 에서 카탈로그와 함께 정리된다.
  - **옛 저장값** — `lib/store/self-learning.ts` v0(전역 한 통 · `ot_*` 등록 · 연속학습 **카운터**)는 v1(사용자별 · `class_bots.id` · **날짜 배열**)로 **번역 대응이 없다.** 옮기지 않되 **지우지도 않는다** — 원본은 `legacyV0` 로 남는다.
  - `2026-06-23_classbot-dual-mode-design.md` — **2026-09-09 개정 박스 신설.** 종전/이제를 표로 갈랐다. **박스는 Locked decisions 목록만이 아니라 본문 전체보다 우선한다** — Goal·§1·§2·§4·§5·§6·§7·§8 이 모두 「전역 토글 + 모드별 홈 + 공식 튜터」 위에 쓰였기 때문이다. 그래서 박스에 **절별 대체표(§⑤)** 를 실어 **어느 서술이 죽고 어느 것이 사는지**를 절 단위로 적고, 그 아홉 절 머리마다 대체표로 보내는 한 줄을 달았다. 본문을 지우지 않은 이유는 그것이 **왜 그렇게 지었는지의 기록**이기 때문이다 — 구현 지시가 아니라 이력으로 읽는다.
  - `03-features-and-ia.md` **§ 2.1** — `/classbot/chat` 줄을 「모드별 봇 목록」에서 **「반 봇 + 담은 봇 한 목록」**으로, `/classbot/my-bots` 를 「자기주도가 사는 **장소**」로. 「자기주도 모드 진입」 메모는 **여는 방법이 바뀌었다**로 다시 썼다(`StudentModeToggle` 과 모드 분기는 되살리지 않는다). `/classbot/discover` 는 내용이 교사 공유 봇으로 갈리고 **nav 도 되살아난다**(「내가 담은 봇」 바로 뒤) — 레일을 닫아 뒀던 까닭은 화면이 mock 「공식 튜터 마켓」이라 라벨과 도착지가 어긋난 것이었고, 그 까닭이 이 PR 로 사라진다. 빈 상태 안내에만 걸어 두면 **이미 반·담은 봇이 있는 학생은 그 안내를 다시 안 봐서 새 봇을 찾을 길이 없다.** 하단탭은 기획 보류로 줄여 둔 셋 그대로다.
  - **인도 번호 정정** — 학생 `/classbot/{classroom,my-bots,discover,discover/[botId]}` 의 인도를 **#268 → #283** 으로. #268 은 base 브랜치가 #267 squash 로 사라져 닫혔고, 같은 내용을 `dev` 위로 리베이스한 것이 #283 이다. **학부모 라우트의 `[예정] #268` 표기는 이 PR 이 손대지 않는다** — 그 화면은 #281 로 이미 `dev` 에 있고, 그 정정은 학부모 쪽 별건이다.
  - **범위** — `proc/spec/**` 문서만. 코드는 건드리지 않는다.

- **2026-09-09 (마이그레이션 번호 예약을 걷는다 — 예약이 틀리면 표가 조용히 사라진다)**: 명세가 **어느 PR 이 어느 마이그레이션 번호를 쓸지 미리 못 박아** 두었고(`self_enrollments` `0005` · `self_study_days` `0006` · `revoked_at` `0007`), 스택의 base 가 squash 머지로 사라져 순서가 갈리자 그 예약표가 **낡은 채로 권위** 노릇을 했다. 리뷰가 그것을 근거로 #280 에 「예약된 `0007` 로 옮겨라」를 세 라운드 반복했는데, **그 요구대로 옮기면 더 나쁜 버그가 난다.** 코드가 아니라 **문서를 사실에 맞춘다.**
  - **실측(일회용 DB · 실행 후 삭제)** — drizzle 은 파일명도 저널 `idx` 도 보지 않고 **`when`(created_at) 하나로** 적용 여부를 가른다(`drizzle-orm@0.36.4/pg-core/dialect.js`: `Number(lastDbMigration.created_at) < migration.folderMillis`). ① #280 의 마이그레이션을 `0007`(`when=1788843161576`)로 먼저 적용한 DB 는 나중에 합류하는 `0005`(`1788412277498`)·`0006`(`1788414652159`)를 **건너뛴다** — `self_enrollments`·`self_study_days` 가 영영 안 생기고, CLI 는 그대로 `migrations applied successfully!` 를 찍는다. ② 파일명만 뒤로 밀고 `when` 을 두어도 **같은 스킵**. ③ #280 이 `0005` 를 쓰고 스택이 리베이스에서 재번호 + `when` 상향을 함께 하면 **전부 순서대로 적용된다.**
  - `2026-09-09_migration-numbering.md` **신설** — 규칙 넷을 세운다. ① **번호는 `dev` 에 먼저 도착한 PR 이 가져간다**(명세는 DDL 을 적고 번호는 적지 않는다). ② **아직 `dev` 에 없는 마이그레이션만 재번호하고, 그때 `when` 도 함께 올린다.** ③ **`dev` 에 들어간 마이그레이션은 동결** — 파일명도 `when` 도 바꾸지 않는다(바꾸면 그것을 적용한 DB 가 같은 SQL 을 다시 돌려 `42P07` 로 죽는다). ④ **`consent_logs.type` 처럼 「값만 느는」 변경에는 마이그레이션이 없다** — 실제 DB 의 `type` 은 CHECK 도 PG enum 도 없는 `text` 다. 소스 인용과 위 실측표를 함께 실었다.
    **재번호의 대가도 실측해 § 3.2 에 적었다** — 리베이스 **전** 브랜치를 돌려 본 개발 DB 는 `when` 이 올라간 순간 같은 `CREATE TABLE` 을 다시 내 **`42P07` 로 멈춘다**(D). 저널 행의 `created_at` 만 손으로 고치는 우회는 더 나쁘다 — migrate 는 통과하는데 **더 작은 `when` 을 가진 앞 번호가 통째로 스킵된다**(E, `revoked_at` 이 빈다). 그래서 전환 절차는 **DB 재생성**이다(F). `IF NOT EXISTS` 류 멱등 DDL 은 부분 완화일 뿐 이 절차를 대신하지 않는다.
    **왜 `2026-05-18_be-api-design.md` 에 절을 더하지 않았나** — 그 파일은 루트 `CLAUDE.md` § 2 「공통 문서 (read only)」가 명시적으로 읽기 전용으로 지정한다. 마이그레이션 정책(§ 6)·Drizzle+TypeORM 공존(§ 6.2)의 권위는 **그 문서에 그대로 두고**, 그것이 정하지 않은 한 가지(「번호를 누가 정하는가」)만 별도 문서로 세워 상호 참조한다.
  - `2026-06-23_classbot-dual-mode-design.md` **개정 박스의 표** — 세 자리에서 번호 예약(`0005`·`0006`·`0007`)을 걷고 **어떤 DDL 이 오는지**만 남겼다. 「학부모 × 자기주도」 줄의 인도 주체도 갈랐다 — 서버(`consent_logs` 축 둘 · `revoked_at`)는 **#280**, 학부모 화면은 #271. 「마이그레이션이 `0000`~`0003` 까지」라는 낡은 서술도 걷었다(`dev` 는 `0004` 까지다).
  - `05-business-rules.md` **§ 11.4** — `revoked_at` 을 「#271(`0007`)」로 적던 **두 자리**를 **#280**(번호 없음)으로. 축 둘을 「#271 이 enum 에 넣는다」로 적던 자리도 고쳤다 — **#280** 이 더하고, **enum 도 마이그레이션도 아니다**(테두리는 `lib/db/schema.ts` union · `lib/mock/family.ts` 의 `ConsentType`·`consentTypeMeta` 셋뿐이고 항상 같이 고친다).
  - **범위** — `proc/spec/**` 문서만. 코드도 `drizzle/` 도 건드리지 않는다.

- **2026-09-07 (보호자 영역 — 가이드가 스펙을 따라잡는다)**: `03 § 2.3` 이 학부모 화면의 인도를 #268·#271 로 배정했는데, 작업 가이드(`CLAUDE.md`)는 여전히 「보호자 영역 코드 추가 **금지**」로 남아 있어 **같은 스냅샷 안에서 두 문서가 반대로 말하고 있었다.** 리뷰가 그 모순을 짚어 두 PR 이 막혔고, 낡은 쪽(가이드)을 갱신한다. **사용자 명시 승인(2026-09-07).**
  - 루트 `CLAUDE.md` — 금지 목록에서 **보호자를 뺀다.** 대신 여는 조건을 적는다: 자녀 동의 뒤 · 실제 로그인 학부모는 아직 진입 불가(claim union 에 `parent` 없음 · OS SSO 가 student 로 내림) · 여는 것은 `packages/types` + SSO 매핑을 넓히는 별건 PR.
  - `apps/classbot/CLAUDE.md` — § 1 표에 학부모 라우트를 넣고 셸 `Role` 을 `student | teacher | parent` 로(§ 5 에 승인 기록), § 2 「사라진 영역」에서 보호자 줄을 걷는다. § 5 의 편집 자유 범위에도 `app/(parent)/parent/*` 를 더한다.
  - `apps/classbot/AGENTS.md` — 도메인 범위 한 줄에 학부모를 더한다.
  - `2026-07-03_m1-sso-identity-mapping.md` — SSO 가 parent 를 거부하는 **근거를 바꾼다.** 「보호자 표면이 없다」는 전제가 끝났으므로, 이제 근거는 claim union 에 `parent` 가 없다는 것이다(그래서 개발용 신원 전용).

- **2026-09-04 (학부모 열람 동의 · 빈 방에 내기 · 자기주도 보류 해제 — `[예정]` 표기)**: 스택 PR **#266~#271** 이 인도할 네 자리를 명세로 먼저 세움. **문서가 먼저 머지되고 구현 PR 이 그 뒤에 온다**(리뷰가 base 스냅샷 기준으로 수렴하게 — 2026-08-24 #223 · 2026-08-24 [봇 관리] 항목과 같은 방식).
  **읽는 법** — **`[예정]` 표기가 붙은 것**이 `dev` 에 없는 것이다. 그 표기는 문장·표 칸 단위로 붙고, 붙는 자리가 줄 전체일 때도(라우트·API·마이그레이션이 없다) 줄 안의 한 조각일 때도 있다(그 자리는 있는데 내용·연결만 바뀐다). 표기 없는 서술은 `dev` 시점 사실이다. 그 PR 이 `dev` 에 들어오면 표기를 지운다. `[예정]` 이 붙은 자리는 **현재 동작 설명이 아니라 그 PR 이 통과해야 할 기준**이다(`12` 의 `[지금]`/`[예정]` 규칙과 같다).
    **`dev` 에 이미 있는 것을 `[예정]` 로 밀지 않는다** — 자기주도 축은 **부품이 있고 문이 닫혀 있다**. 있는 것: `/classbot/discover`·`/classbot/learn/[tutorId]{,/[unitId]}` 라우트, `lib/store/self-learning.ts`(goal·unitProgress·streak), `useModeBots()` 의 self 분기, `tutor-market-card` 의 등록·해제. **닫힌 것: self 모드로 들어가는 길** — 「기획 보류」로 헤더 토글(`StudentModeToggle`)이 `app-header.tsx` 에서 비노출이고 `useStudentMode()` 의 default 가 `class` 로 고정돼 있어, 저장값이 없는 학생은 self 를 볼 수 없다. **그 문을 여는 것도 이 시리즈의 몫**이지 「이미 된 것」이 아니다. 이 시리즈가 새로 내는 것은 그 진입 복구 + **뒤를 받칠 서버** + **교사 공유 마켓 · 수업방 · 학부모 화면**이다.
  - `05-business-rules.md` **§ 11.4 신설** — 학부모 열람 동의(`consent_logs`) 축 6종을 한 표로. **학생 동의가 최종 관문이라는 § 11.3 · 04 UC-T1 7단계 · 13 § 4.6 의 계약은 개정하지 않는다** — 여섯 축 전부 학생 동의 필수다. 이 절이 더하는 것은 축 둘과, **그 둘에는 교사·기관 발송 승인이라는 위 관문이 없다**는 사실이다(두 관문을 갈라 적고, 두 칸은 `and` 다). 그리고 규칙 넷(조회 조건 안 · 미동의와 무활동을 못 가름 · `revoked_at` 철회 · 부모의 열람이 학생 화면으로 되돌아가지 않음 — 서버 계측은 이 절이 금지하지 않는다).
    **넓힌 이유** — 자기주도에는 **승인할 교사가 구조적으로 없다.** 그래서 그 축의 승인 주체가 학생 본인이 되고(그래서 새 축이 생겼다), 같은 이유로 **반·과제도 같은 게이트 뒤로** 옮기는 것이 사용자 결정(2026-09-04)이다.
    **권한이 넓어진 것이 아니라 게이트가 생긴 것이다** — 반·과제 현황은 종전에 **자녀 링크만 있으면** 열려 있었고 이제 **학생이 켜야만** 열린다. 자기주도 요약도 처음부터 동의 뒤에 둔다. 「학부모가 더 많이 본다」로 읽으면 사실과 반대다 — 켠 것이 없으면 아무것도 못 본다.
    **`input/docs-archive` 갱신 범위** — `07_풀림_클래스봇_핸드오프.md` **§ 1.3 P3** 만(문서 버전 v1.2 → v1.3). 이 폴더는 리포 `CLAUDE.md` 상 read-only 권위라 **사용자 명시 승인**(2026-09-04)을 받고 손댔고, 승인이 P3 로 한정되어 **그 밖은 건드리지 않았다.** 같은 승인으로 `02-product-definition.md` **§ 4.3**(학부모 권한 문구)도 함께 갱신했다 — 파생 스펙과 권위가 갈리지 않게 **한 변경세트로** 넣는다.
    **그 밖** — `class_assignment_summary` 가 새 축이다(승인 주체를 옮긴 것이 아니라 자녀 링크만으로 열리던 자리를 § 11.3 에 맞춘 것이다). `dev` 에 있는 것은 `consent_logs` 표(축 다섯)·`parent_child_links` 뿐이고, 새 축 둘·`revoked_at`·열람 라우트는 **`[예정]` #267·#271** *(**`[2026-09-09 정정]`** 새 축 둘과 `revoked_at` 을 실제로 인도하는 것은 **#280** 이다. 종전에 여기 붙어 있던 마이그레이션 번호 `0007` 도 걷었다 — 아래 2026-09-09 항목)*. § 11.1 에 항목 둘 — `lib/current-user.ts`(현재 사용자 해석기: JWT 서명 검증 + 데모 폴백 `student_001`)는 **`dev` 에 이미 있으므로 사실로** 적고, 그 위에 한 겹 더 얹는 **개발 전용 신원 폴백**(`lib/dev-identity.ts` · `pullim_dev_identity` · 인증이 아니라 「명의」 · prod 비노출)만 **`[예정]` #266** 으로 갈랐다, § 11.2 의 `/parent/*` 를 Future 에서 **`[예정]` #268·#271** 로.
    **번호 충돌 정정** — 종전 § 11.4 「API 호출 비용 통제」는 **§ 11.5 로 민다.** 이 절을 가리키는 바깥 참조는 없었다.
  - `03-features-and-ia.md` **§ 2.1·2.2 라우트 인벤토리** — 이 시리즈가 낼 화면 아홉을 **`[예정]` 로** 등재: 학생 `/classbot/{classroom,my-bots,discover/[botId]}`(#268) · `/classbot/me/share`(#271) · 교사 `/teacher/{classroom,marketplace,marketplace/[botId]}`(#269) · 학부모 `/parent{,/assignments}`(#268) · `/parent/self-study`(#271).
    **이미 있는 둘은 줄 전체를 `[예정]` 로 밀지 않고 바뀌는 조각만 표시했다** — `/classbot/discover` 는 `dev` 에서 이미 「공식 튜터 마켓」(mock 공식 튜터 셋 + 「곧 만날 봇」 예고)을 그리고 nav 에만 안 걸려 있다. #268 이 그 내용을 **교사가 공유한 봇**으로 갈아끼우고 nav 를 되살린다. `/classbot/chat` 도 이미 `useModeBots()` 로 모드별 봇 목록을 그리고, self 목록이 자기 등록 튜터를 받는 **배선까지 이미 있다**(`tutor-market-card` 가 `self-learning` 에 등록·해제를 쓴다). 그 목록을 학생이 실제로 보게 되는 것 — self 모드 진입 복구와 마켓 봇을 그 목록에 얹는 것 — 이 #268 이다. 그리고 **이미 있는데 인벤토리에 빠져 있던** `/classbot/learn/[tutorId]{,/[unitId]}` 두 줄을 사실로 채웠다.
    **곁들여 바로잡은 낡음(교사 쪽 둘)** — ① `/teacher/grading` 줄과 **메모 3** 이 「기본 = 채점 대기 큐 · 학생 목록도 `?view=` 탭도 학생 상세 링크도 없다」로 적고 있었으나 **셋 다 `dev` 에 있다**(기본은 `?view=students`, 큐는 `?view=queue` 탭, 학생 줄은 `?from=grading` 을 달아 보낸다). ② **메모 4** 의 「링크하는 곳은 셋 · 셋 다 source 를 안 넘긴다 · `?from=` 은 미구현 계약」은 셋 다 낡았다 — **다섯**이고(관제소 · 교사 홈 · 채점 허브 학생 탭 · **채점 상세의 「대화 기록」** · 리포트 명단), 그중 **넷**이 `from` 을 달고, **받는 쪽도 읽는다**(`students/[id]/entry-source.ts` 의 `resolveEntrySource()`/`entryTarget()`. 모르는 값은 `monitor` 로 떨어진다). 채점 쪽은 `grading`(학생 탭)과 `grading-queue`(채점 상세)로 **갈라져 있다** — 검수하던 큐로 돌아가야 하기 때문이다. 남은 것은 리포트 명단이 값을 안 붙이는 한 자리. 둘 다 이 PR 이 만든 차이가 아니라 문서가 뒤처져 있던 것이다. **[`11-grading-hub.md` § 1.5](11-grading-hub.md) 는 같은 낡은 서술을 그대로 들고 있고, 이 PR 범위 밖이라 손대지 않았다 — 별도 문서 PR 사안이다.** § 2 머리에 `[예정]` 표기 규칙을 세웠다 — **`[예정]` 라우트로 먼저 링크하지 않는다.**
  - `03-features-and-ia.md` **§ 2.3** — 학부모 영역을 **`[예정]`(개발·데모 한정)** 으로. `dev` 에 `app/(parent)` 트리가 없다는 사실을 먼저 적고, 들어온 뒤에도 실제 로그인 학부모가 못 들어오는 이유(claim union 과 BE `user-role.enum.ts` 둘 다 `parent` 없음 · OS SSO 가 student 로 내림)와 선행 조건(별도 PR)을 명시. 그 PR 전까지 학부모는 **역할로서 미구현**이다. **홈 경로도 맞췄다** — `04` UC-P1 이 `/parent/dashboard` 로 적고 있었으나 인도되는 것은 `/parent` 라(`app/(parent)/parent/page.tsx`) 04 쪽을 고쳤다. 문서마다 첫 진입 경로가 갈리지 않게 하는 것이 목적이고, `dashboard` 세그먼트는 다른 역할 홈(`/teacher` · `/classbot`) 어디에도 쓰지 않는다.
  - `14-teacher-assignment-workspace.md` **§ 5.1** — 「대상 학생 1명 이상」 제약을 **걷음(#269 — `dev` 반영 완료)**. 참여 코드 도입 후 「반을 먼저 열고 학생이 뒤에 들어오는」 상태가 정상이 됐다. 대신 **명단을 못 읽은 상태**는 내기 불가(그 빈 배열은 「학생 0명인 반」과 모양이 같아 전원에게 내기로 바뀐다). `assignment-form.tsx` 가 `rosterUnknown` 을 세우고 `targetValid` 를 새 식으로 쓴다 — 2026-09-14 에 `[지금]`/`[예정]` 표기를 걷었다.
  - `2026-06-23_classbot-dual-mode-design.md` — **FE-only · mock-first 보류를 푸는 것이 정해짐**. deferred 였던 BE persistence(#270) · auth-scoped self-enrollment(#266+#270) · teacher publish(#267+#269) 는 **`[예정]`** 이고 `dev` 마이그레이션은 아직 `0003` 까지다 *(**`[2026-09-10 정정]`** BE persistence(#270)는 **`dev` 에 들어왔다** — 표·라우트까지다. 그 라우트를 부르는 **훅·스토어의 서버 정본 전환은 `[예정]` #273** 이고, 종전에 이 자리는 그 인도를 적지 않아 규칙만 남아 있었다 — 아래 2026-09-10 항목)*. **목표·단원 진행(P5)은 이 시리즈가 손대지 않는다** — 다만 「미구현」도 「테이블이 비었다」도 아니다. FE 는 mock-first 로 돌고(`officialTutors` 커리큘럼 · `self-learning` 스토어의 goal·unitProgress·streak · `/classbot/learn/[tutorId]`), `bot_curriculum_units` 에는 **`scripts/seed.ts` 가 `botCurriculum` 을 넣는다**. 남은 것은 **경로**다 — 교사가 만든 실제 봇에 커리큘럼을 붙이는 publish 쪽도, 그 표를 읽어 학습 화면에 대는 read 쪽도 없다. FE 는 여전히 mock 을 읽는다.

- **2026-05-06**: 기존 `docs/` 11개 문서 + `pullim-study-screens` 스킬을 본 spec으로 합성 (역설계 마이그레이션)
- **2026-05-06**: UX/디자인 베이스라인 보강 — `04 § 6.6` 오버플로 처리 규칙 신설, `08 § 7.3` 버튼 어포던스 규칙 신설, `07 § 5.2` 학술 기호·약어 매핑 추가, `07 § 6` UX writing(한자어 정책) 신설. 트리거: `/q/infinity/solve` 시험 모드 다이얼로그 viewport 초과 버그, `/q/review` "정복 세트 풀이" 버튼 어포던스 미달, θ 등 학술 기호 노출 + 한자어("잔존" 등) 사용성 이슈.
- **2026-05-07**: 명세 회귀 사례 closing — `04 § 6.6.3/6.6.4` (오버플로·다이얼로그 footer cleanup), `08 § 7.3.5` (버튼 어포던스 회귀: Q·라이브러리·클래스봇·플래너 처리 완료), `07 § 5.2` (학술 기호 회귀: 2026-05-06 시점 5건 + 2026-05-07 시점 신규 도메인 발견 처리 완료) 갱신. [2026-05-07 spec-regression-closing plan](../archive/2026-05-07_spec-regression-closing.md).
- **2026-08-24 (「버튼은 단어로」)**: `07-branding.md` § 6.6 「버튼은 단어로」 · § 6.7 「부제는 사실만 남긴다」 신설 — 버튼·링크·CTA 의 보이는 글자는 단어(두 단어까지), 잃은 뜻은 `aria-label` 로 보존. § 6.6.3 이 클래스봇 버튼 이름 권위 표다. 적용 범위는 **클래스봇 도메인 화면**(`apps/classbot`)으로 한정 — 플래너·Q 카피는 이 리포에 구현이 없어 손대지 않는다. **문서가 먼저 들어가고 구현은 뒤따르는 FE PR 이 맞춘다.**
  함께 맞춘 spec — `03-features-and-ia.md` § 4.3 · `04-ux-flow.md` UC-T1 · § 9.11 · `11-grading-hub.md` · `12-student-assignment-solve.md` · `13-reports-and-emotion-checkin.md` § 3.2 · § 3.3.2 · § 3.3.3 · § 8 · `08-design-system.md` § 15.1.3 · `2026-06-23_chat-guided-lesson.md` · `2026-06-25_classbot-replay-depth-design.md`.
  **줄이지 않기로 한 라벨** — 「가볍게 가기」·「평소대로 보기」·「자세히 보기」·「개념 다시 보기」·「이어서 하기」(§ 6.6.2(6) 떼면 뜻이 무너짐) · 챗 빠른 칩 「개념 더보기」·「해설 보기」(§ 6.6.2(5) 학생 말투) · 「전체 보기」(같은 화면 「전체」와 충돌) · 플래너·Q 의 「지금 시작하기」(적용 범위 밖).
  **곁들여 바로잡은 낡음** — `12-student-assignment-solve.md` 는 라우트가 하나도 없던 2026-05-11 에 쓰여 § 2.1 「워크스페이스가 없다·깨진 링크」와 § 3.2 「홈과의 관계」(`PrimaryAssignmentCard` 등)가 현행과 어긋나 있었다. 소스로 확인해 고치고, 문장마다 `[지금]` / `[예정]` 시점 표기를 세웠다. `11-grading-hub.md` 의 「최종 점수만 확인」도 결과 페이지 실제 구성으로 정정.
  **`input/docs-archive` 갱신 범위** — `07_풀림_클래스봇_핸드오프.md` **§ 4.1** 과 `05_풀림_수업방_세부기획.md` **§ 8.1** 만. 둘 다 **사용자 명시 승인**(2026-08 재편 2차와 같은 자리)이고, 그 밖은 건드리지 않았다.
  **아직 어긋난 채 남는 것** — 핸드오프 § 10.2 KPI · § 11 MVP 는 옛 이름 「이대로 만들기」를 그대로 쓴다. 승인이 § 4.1 로 한정돼 손대지 않았다. **별도 승인 사안**이다.
- **2026-05-11**: 추출본 누락 영역 명세 신설 — `11-grading-hub.md` (채점 허브), `12-student-assignment-solve.md` (학생 과제 풀이 — Q 도메인 의존 제거 + 클래스봇 내부 자급), `13-reports-and-emotion-checkin.md` (리포트 + 감정 체크인 + 웰빙 지수). 권위 문서 `07_풀림_클래스봇_핸드오프.md` § 4.1·4.7·4.8·4.9 기반. 우선순위는 채점 허브 → 풀이 → 리포트 순.
- **2026-05-11**: 브랜드 표면 명칭 정렬 — `07 § 1` (서비스명/슬로건/미션 표 재구성: 모브랜드 "풀림" / 표면 "풀림 클래스봇" 분리), `07 § 1.4` (메타데이터 표준 신설 — title·description·OG·manifest 단일 진실원), `02` 추출본 컨텍스트 박스 추가. 트리거: Vercel 임베드에 "풀림 스터디" 노출. [plan 2026-05-11_brand-classbot-metadata](../archive/2026-05-11_brand-classbot-metadata.md).
- **2026-05-11**: E2E 진입점 명세 신설 — `14-teacher-assignment-workspace.md` (교사 과제 생성·내기 워크스페이스). spec 11/12/13이 사이클 후반을 채웠다면 14는 시작점. mock store(Zustand + localStorage) 기반으로 백엔드 없이 끝에서 끝까지 시연 가능하도록 설계. 권위 문서 Flow C 1단계 구체화.
- **2026-05-19**: 클래스봇 사적 디자인 감사 22개 항목(P0~P3) SPEC 반영 — 출처 `input/design-system/private-classbot.md` + 통합 시스템 v0.1 (`DESIGN_SYSTEM.md` / `tokens.css`). 4개 SPEC 갱신:
  - **08-design-system.md** — § 1.2.1 메타 컬러 사용 가이드(#97A0B4 vs #6B7489), § 3.5 8단계 타이포 토큰, § 4.1 radius 3+pill 통일 권고, § 5.1 포커스 링 토큰, § 10.1~10.3 모션 토큰 + reduced-motion, § 12 a11y 보강(hit-area·skip-link·aria-label), § 15 챗봇 도메인 디자인(메시지 버블 3 variant·시간 디바이더·메시지 타입 6종·시그니처 모션 9종·LIVE 카드·봇 스위처·봇 메타 카드·과제 카드 상태별 컬러·입력바·신규 컴포넌트 10개)
  - **07-branding.md** — § 3.2 DON'T에 Scope/Tier 코드 금지 추가, § 4.6.1 봇별 시그니처 인사·§ 4.6.2 봇 5종 단일 진실원 표·§ 4.6.3 시간대별 인사, § 4.10 클래스봇 온보딩 학생 시선 카피, § 5.3 Scope L1~L5·Tier T1~T3 학생 노출 룰 신설, § 8 체크리스트 § 5.3 항목 추가
  - **04-ux-flow.md** — § 9 클래스봇 UX 패턴 신설 (홈 4블록 우선순위·봇 카드 정보 보강·봇 메타 카드 collapse·봇 스위처 칩·**모바일 키보드 visualViewport sticky Critical**·빠른 칩 동적 추천·입력바 첨부/음성·메시지 그루핑·첫 응답 personalization·LIVE/리플레이/16:9 썸네일·온보딩 인터랙티브 데모·브레드크럼 자동 dedupe·22개 항목 P0~P3 매핑 표)
  - **13-reports-and-emotion-checkin.md** — § 3.3.3 웰빙 허브에 담당 봇 코멘트 카드, § 3.3.4 체크인 사후 봇 반응, § 9.1.2 7일 막대 컬러 매핑, § 9.2 5지표 펼침 패턴, § 9.3 담당 봇 코멘트 카드 토큰, § 9.4 풀림 무드 이모지 셋(P3), § 9.5 신규 컴포넌트 2종 추가
- **2026-06-02**: auth(이메일/비밀번호 + JWT) 머지 정합 — Ph7/Ph8 조기 인도 + TypeORM·Drizzle 마이그레이션 공존을 spec에 반영. 출처: auth PR #87(shared)/#88(BE)/#89(FE). **컨트롤타워가 수용한 예외**(Ph8 결정·Ph7 데이터 레이어가 일정보다 먼저 인도)를 문서 레벨에서 기록 — 사용자/G1 비준 대기. 4개 SPEC 갱신:
  - **2026-05-18_be-api-design.md** — §5 Ph7(fetch 데이터 레이어 조기 인도)·Ph8(결정·인도 완료) 상태 갱신, §3 인증 컨벤션에 `/auth/*` JWT 명시, §6 결정보류 해소 표시, **§6.1 Ph8 인증 인도 결정**(자체 구현·`auth_*` 테이블·서명 매요청 검증·admin 부여 차단·refresh 회전+블랙리스트) + **§6.2 Drizzle+TypeORM 마이그레이션 공존 노트**(경계·근거·후속 통합 플래그) 신설
  - **05-business-rules.md** — § 11.1 세션을 "JWT access/refresh 자체 구현"으로 구체화(미상세 placeholder 해소)
  - **09-tech-stack.md** — § 14 데이터 저장에 auth 백엔드(NestJS+TypeORM) 인도 사실 + ORM 공존 행 + 토큰 블랙리스트 Redis→Postgres 비고 추가
- **2026-08-24**: 봇 빌더(`/teacher/builder`) **8단계 위저드 → 한 길 · 세 마당** 재편을 기록. 구현은 **PR #223** 이 인도하고 `dev` 시점 코드는 아직 8단계 위저드다 — #223 머지 후 상태 표기를 지운다. 문서가 8단계를 못 박고 있어 #223 이 「명세 회귀」로 읽히던 상태를 푸는 것이 목적. 갱신 범위:
  - **03-features-and-ia.md** — § 2.2 사이트맵 한 줄, **§ 4.3 전면 개정**(마당 셋 + 만든 뒤 화면 · 8→3 대응표 · 설계 원리 2가지 · 「없어진 것」 · **「교사가 참여 코드를 확인·공유하는 자리」**). 다른 문서가 「빌더 § N 단계」로 가리키던 자리는 이 대응표를 경유해 읽는다
  - **04-ux-flow.md** — UC-T1 2·3단계 (배포 관문 → 만든 뒤 화면, 빌더발 코드 생성·링크·QR 는 미구현 명시)
  - **05-business-rules.md** — § 10.1 봇 이름·답 범위·루브릭 3행 (루브릭은 빌더에서 과제 출제로 이관)
  - **07-branding.md** — § 4.7 빌더 시작 마이크로카피
  - **09-tech-stack.md** — § 앱 트리 `builder/page.tsx` 주석
  - **10-roadmap.md** — § 2.1 봇 빌더 행 + § 트리 1.4
  - **12-student-assignment-solve.md** — § 5.1 Scope 우선순위 (정하는 자리는 봇 설정으로 옮겼고, 풀이 화면이 그 값을 **소비하는 경로는 미구현**임을 갈라 적음)
  - **13-reports-and-emotion-checkin.md** — § 5.1 웰빙 가중치 · § 5.4 학부모 자동 승인 정책 · § 연계 표. 「빌더 § 7 (안전)」 소유를 봇 설정으로 옮김(지금은 봇별 설정 `/teacher/bots/[botId]`). 셋 다 **미구현**이며 빌더에도 없었다
  - **14-teacher-assignment-workspace.md** — § 1 · § 2.2 G4 · § 3.1 M2·C2·C4 · 문항/필드 표 · 카피 · § 3.2 진입점. 빌더 산출물 소비 범위를 **실제 구현(단원 하나)** 으로 좁힘 — 모드는 고정 기본값이고 톤·답 범위는 소비하지 않는다
  참여 코드 — 배포 관문을 없앤 뒤 「교사가 학생에게 무엇을 주나」를 03 § 4.3 에서 못 박았다: **코드는 반에 붙으므로 확인·공유 자리는 빌더가 아니라 봇 운영 화면(`/teacher/classbot`)의 반 카드**. 현재 미구현이며(종전 8단계의 「8. 배포」도 코드를 보여주지 않았다 — 이번 재편이 만든 빈자리가 아니다) 반 카드 구현은 별도 과제.
  **`input/docs-archive` 갱신 범위** — `07_풀림_클래스봇_핸드오프.md` § 3 IA 트리 + § 4.1 만 갱신했다(v1.0 → v1.1). 이 폴더는 리포 `CLAUDE.md` 상 read-only 권위라 **사용자 명시 승인**을 받고 손댔고, 승인이 § 4.1 로 한정되어 그 밖은 건드리지 않았다. 종전 8단계 표는 `<details>` 로 원문 보존.
  **아직 어긋난 채 남는 것** — `05_풀림_수업방_세부기획.md` § 8.1 `Step 6: 학생 초대 → 코드/링크/QR 생성` · 체크리스트 「봇 빌더 위저드 (6단계)」. 승인 범위 밖이라 손대지 않았다. **별도 승인 사안**이며, 그 문서를 읽는 사람은 이 개정과 어긋나 있다는 사실을 알고 읽어야 한다.
  *(뒤이은 **2026-08 재편 2차**에서 `05_풀림_수업방_세부기획.md` § 8.1 이 **사용자 명시 승인**으로 갱신 범위에 들어왔다 — 그 문서 § 8.1 의 개정 주석이 근거다. 이 줄은 그 이전 상태를 적은 것이다.)*
  함께 바로잡은 것 — 종전 문서가 **미구현을 구현된 것처럼** 적고 있던 것들. ① 「배포」 산출물의 코드·링크·QR — 학생의 참여 코드 입력 → enrollment 경로는 있으나(`lib/mock/class-codes.ts` + `joinClass()`), **빌더가 코드를 만들어 보여주는 흐름과 링크·QR 은 구현된 적이 없다** ② 루브릭 5축(빌더 로컬 state, 진실원 아님) ③ 임시저장(저장 없는 데모). 셋 다 #223 과 무관하게 `dev` 시점에도 사실이다.
- **2026-08-24**: 「봇 설정」(`/teacher/settings`) → **[봇 관리]** 이름 변경 + **봇 목록(`/teacher/bots`) → 봇별 설정(`/teacher/bots/[botId]`)** 구조 변경을 기록. 봇은 여럿인데 설정 화면이 하나뿐이라 「지금 어느 봇을 고치고 있나」가 화면에 없던 것이 트리거. 문서가 먼저 들어갔고 구현은 **#243** 이 인도했다 — `/teacher/bots` 목록과 `/teacher/bots/[botId]` 봇별 설정이 `dev` 에 있고 링크 넷도 옮겨졌다. **딥링크 둘(`monitor-roster.tsx` · `build-yards.tsx`)은 아직 옛 경로를 쓴다**(아래 「옛 경로」 참고). 갱신 범위:
  > ⚠️ **이 항목은 문서가 구현보다 먼저 들어가던 시점의 기록이다.** 아래 하위 불릿의 「아직 없다」·「미구현」·「지금도 본체」는 **그때의 상태**이고 지금은 아니다 — 구현은 **#243** 이 인도했다(아래 같은 날짜의 「[봇 관리] 구현 인도」 항목). 현재 상태는 `03 § 4.4` 를 봐라.
  - **03-features-and-ia.md** — § 2.2 사이트맵 세 줄 + 메모, **§ 4.4 신설**. 절을 일곱으로 갈라 **「지금(`dev`)은 이렇다」(§ 4.4.1)와 「이 개정이 정하는 것」(§ 4.4.2~)을 문장마다 나눠 적었다** — § 4.4.1 은 소스로 확인한 사실만, 나머지는 「~하기로 한다」. § 4.4.5 는 새 버튼 이름을 `07 § 6.6` 「버튼은 단어로」에 걸어 뒀다(「새 봇」 · 「봇 만들기」). 종전 § 4.4~4.10 은 § 4.5~4.11 로 한 칸씩 밀렸다
  - **09-tech-stack.md** — § 앱 트리의 `settings/page.tsx` 는 **지금도 본체**로 두고, 그 아래에 「FE PR 이 들어오면 `bots/` 가 생기고 이 파일은 넘겨보내는 자리만 남는다 — 아직 없다」를 주석으로 달았다. 없는 디렉터리를 트리에 그리지 않는다
  - **10-roadmap.md** — § 2.1 교사 클래스봇 행. 2026-04-29 락 해제 기록(`settings`)은 그대로 두고 그 뒤에 「앞으로의 정의는 `bots`」를 **미구현 표시와 함께** 갈라 세웠다
  - **12-student-assignment-solve.md** — § 5.1. 「정하는 자리는 `dev` 시점에 `/teacher/settings`, 뒤에 `/teacher/bots/[botId]` 로 갈린다(미구현)」로 현재·예정을 갈라 적음
  - **13-reports-and-emotion-checkin.md** — § 5.1 · § 5.4 · § 연계 표의 경로·이름 갱신 (셋 다 여전히 **미구현**)
  **역할 분리** — 운영 화면(`/teacher/classbot`)은 「지금 잘 돌고 있나」, 봇 관리는 「이 봇을 어떻게 굴릴까」. 운영 사실(운영 중·멈춤 · 붙은 학급 · 인원 · 낸 과제)은 운영 화면에만 두고 봇 관리 목록에 옮겨 적지 않는다. 카드 컴포넌트는 **공유하지 않고 값의 출처(`classBots`·`scopeMeta`)를 공유한다** — 운영 카드에 운영 사실이 붙어 있어 봇 관리가 쓸 수 없고, 봇 관리 줄에 운영 사실을 넣으면 같은 화면이 둘이 된다.
  **옛 경로** — `/teacher/settings` 는 목록으로 넘겨보내는 자리로만 남기기로 한다(미구현). 지우지 못하는 이유는 다른 작업이 물고 있어 이 개정이 손대지 않는 두 자리(`monitor-roster.tsx` · `build-yards.tsx`)의 `?tab=` 딥링크이며, 그 두 자리가 옮겨오면 함께 지운다. 옮길 링크 넷(`nav-config.ts` · 운영 화면 봇 카드 「더보기」 · 교사 홈 처리 대기 · 학생 기록 「이탈 대응 강도」)은 `03 § 4.4.6` 표에 「지금」·「옮긴 뒤」로 갈라 적었다.
  **`input/docs-archive` 갱신 범위** — `07_풀림_클래스봇_핸드오프.md` § 3 IA 트리(+ 문서 버전 v1.1 → v1.2) · § 4.1 「없어진 것」의 경로 한 줄 · § 4.2 구현 요구사항의 경로 한 줄, 그리고 `05_풀림_수업방_세부기획.md` § 8.1 의 경로 한 줄. read-only 권위 폴더라 **사용자 명시 승인**을 받은 절(클래스봇 빌더 · IA)로 한정했다. § 4.2 의 그 한 줄은 2026-08-24 빌더 재편(#227)이 「정하는 자리만 봇 빌더 § 5 → 봇 설정으로 **이동**한다(§ 4.1 대응표 ⑤)」로 새로 써 넣은 문장이라, 같은 자리의 경로 표기를 이어서 맞춘 것이다 — 새로 여는 범위가 아니다.
  **아직 어긋난 채 남는 것** — `05_풀림_수업방_세부기획.md` § 4(모듈 구조 `bot-builder/ # 노코드 봇 설정 위저드`) · § 8.2(「봇 설정(시간대 스케줄)」)의 **이름** 두 자리. 승인 범위 밖이라 손대지 않았다. 경로는 적혀 있지 않아 깨진 링크는 아니고, 이름만 옛것이다.
  **다음 작업 예고** — [봇 빌더]를 [봇 관리] 하위(`/teacher/bots/new`)로 옮긴다. 이번 개정은 옮기지 않고 자리만 잡아 뒀다(`03 § 4.4` 마지막 절).
- **2026-08-27 ([봇 관리] 구현 인도 + 옛 경로 정리)**: 위 2026-08-24 항목이 「~하기로 한다」로 적어 둔 것이 **전부 코드로 들어왔다.** 그 항목은 문서가 구현보다 먼저 들어가던 시점의 기록이라 그대로 두고, **지금 상태는 여기 적는다.**
  - **두 화면** — `/teacher/bots` 목록과 `/teacher/bots/[botId]` 봇별 설정이 `dev` 에 있다 (**#243**). 카탈로그에 없는 `botId` 는 404
  - **들어오던 링크 여섯을 모두 새 경로로 옮겼다** — 넷은 #243 이(`nav-config.ts` 사이드바 · 운영 화면 봇 카드 「더보기」 · 교사 홈 처리 대기 · 학생 기록 「이탈 대응 강도」), 남아 있던 **딥링크 둘**(`monitor-roster.tsx` `?tab=drift` · `build-yards.tsx` `?tab=safety`)은 **이 개정이** 옮겼다. #243 때 그 둘을 손대지 못한 것은 다른 작업이 물고 있었기 때문이고, 그 작업들이 `dev` 에 들어와 이제 옮겼다
  - **옛 경로 `/teacher/settings` 는 넘겨보내는 자리로 남는다** — `?tab=` 도 그대로 실어 보낸다. 앱 안에서 그리로 가는 링크는 이제 하나도 없고, 남는 까닭은 **앱 밖에서 오는 옛 주소**다
  **§ 4.4.6 의 삭제 조건을 걷었다 (개정이 아니라 정정)** — 위 2026-08-24 항목과 `03 § 4.4.6` 은 넘겨보내는 자리를 남기는 까닭을 **딥링크 둘로만** 적고 「그 두 자리가 옮겨오면 함께 지운다」고 썼다. 그런데 상위 권위인 `07_풀림_클래스봇_핸드오프.md` § 3 은 조건 없이 「옛 `/teacher/settings` 는 목록으로 넘겨보내는 자리로 **남는다** — 앱 밖에서 오는 옛 주소가 404 로 끊기지 않게」라고 적고, `03 § 4.4` 머리도 「**앱 밖 주소만이 아니라** 그 두 링크도 함께 받는다」로 **근거를 둘** 적고 있었다. 파생 스펙이 상위 권위를 옮겨 적으면서 **둘 중 하나(앱 안 링크)만 세고** 삭제 조건을 쓴 것이다. 앱 밖 주소는 앱 안 링크를 옮긴다고 없어지지 않으므로 그 조건은 성립하지 않는다. **삭제 조건을 걷고 파생 스펙을 상위 권위에 맞췄다** — 제품 결정은 07 § 3 이 이미 내린 것이고 바뀐 것이 없다. **2026-08-27 소유자 확인.**
  **갱신 범위** — `03-features-and-ia.md` § 4.4 머리 · § 4.4.6(제목 · 링크 표 여섯 줄 · 정정 근거), `09-tech-stack.md` 앱 트리 주석, `10-roadmap.md` § 2.1 교사 클래스봇 행, `13-reports-and-emotion-checkin.md` 연계 표, 그리고 `input/docs-archive/07_풀림_클래스봇_핸드오프.md` § 3 개정 메모에 「앱 안 링크는 모두 옮겼다」 한 마디. 라우트가 남는 **까닭은 07 이 정한 그대로**라 손대지 않았다.
  **남는 것** — [봇 빌더]를 [봇 관리] 하위(`/teacher/bots/new`)로 옮기는 것은 아직 안 했다 (`03 § 4.4.7`, 코드 두 자리의 `TODO(봇 빌더 이식)` 주석).
