/** 정수 파싱 기수. */
export const DECIMAL_RADIX = 10;

/** Access Token 기본 만료 시간(초) — 1시간. */
export const DEFAULT_JWT_EXPIRATION = 3600;

/** Refresh Token 기본 만료 시간(초) — 14일. */
export const DEFAULT_JWT_REFRESH_EXPIRATION = 1_209_600;
